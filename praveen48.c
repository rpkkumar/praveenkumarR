#include <stdio.h>
int main()
{
   int s,a[50],i=0,sum=0;
printf("enter the limit");
scanf("%d",&s);
printf("enter array element");
for(i=0;i<s;i++)
{
scanf("%d",&a[i]);
sum=sum+a[i];
}
printf("average = %d",(sum/n));

    return 0;
}
