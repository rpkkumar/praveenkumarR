#include<stdio.h>
#include<conio.h>
void main()
{
  int s,p,i ,sum=0;
  scanf(" %d%d", &s &p");
  printf("%d%d",s,p);
  for(i=0;i<n,i++)
  {
    printf("%d",b[i]);
  }
  for(i=0;i<=p;i++)
  { 
  sum=sum+b[i];
  }
  printf(" \s %d",sum);
}  

