#include <stdio.h>
void main ()
{
    int a,r,s;
    
    printf("enter the numbers:");
    scanf("%d",&a);
    printf("the power value:");
    scanf("%d",&s);
    b= pow(a,s);
    printf(" the power value is %d^%d=%d",a,s,r);
    
}
