#include<stdio.h>
#include<conio.h>
int main(void)
{
	int k,x;
	char s[100];
	scanf("%s",s);
	for(k=0;s[k]!=0;++k)
	{
		
	}
	for(int i=0;i<k;i++)
	{
		x=isdigit(s[i]);
		
	}
	if(x!=0)
	printf("Yes");
	else
	printf("No");
	return 0;
}
