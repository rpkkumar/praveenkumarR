#include <stdio.h>
#include<conio.h>
int main() 
{
    int n,a[10],i;
    scanf("%d",&s);
    for(i=0;i<s;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<s;i++)
    {
      printf("%d %d\s",a[i],i);
    }
	return 0;
}
